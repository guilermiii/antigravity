import React from 'react';

/**
 * Contrato do componente LoginScreen (Skeleton para TDD)
 * @param {Object} props
 * @param {() => void} [props.onNavigateToDashboard] - Callback para navegação de retorno ao painel
 */
export default function LoginScreen({ onNavigateToDashboard }) {
  return null;
}
